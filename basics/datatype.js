"use strict"; // Treat all JS code as newer version
// alert ( 3 + 3 ) // We are using node.js not browser. As the engine to run js used to be hidden in the browser. But to alert using node we have to use different syntax
console.log( 3 + 3 )
console.log( 3 * 2 ); console.log( 2 + 4 );
let name = "Chandrima"
let age = 21
let isLoggedIn = true
let state; // this will print the result undefined
let temp = null; // this will always erturn null as value

/*
number = 2 raised to the power 53
bigint - > for bigger numbers
string => ""
boolean
undefined =>
symbol => to impart uniqueness
*/

// Object

console.log(typeof undefined) // undefined
console.log(typeof null) // object
